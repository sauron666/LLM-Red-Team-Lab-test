[project]
name = "llm-attack-lab"
version = "3.0.0"
description = "GUI + CLI LLM red teaming lab with reporting, baseline compare, auth checks, and Burp bridge"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }

[tool.black]
line-length = 100

[tool.ruff]
line-length = 100
